const size = 8;
let s = "";
for (let i = 0; i < size; i++)
{
  if (i % 2 == 0) {
    evenChar = " ";
    oddChar = "#";
  }
  else {
   evenChar = "#";
   oddChar = " ";
  }

  for(let j = 0; j < size; j++)
  {
    if (j % 2 == 0) s += evenChar;
    else s += oddChar;
  }
  
  s += "\n"
}

console.log(s);